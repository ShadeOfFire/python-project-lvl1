name: linter
on: 
     - push
     - pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: install poetry
        run: pip install poetry
      - name: run linter
        run: make lint
